brk1<-.4 #define first break for recruit class
bpts<-hist(t0, breaks="FD")#this bins according to the Freedman-Diaconis rule. 
seq<-c(bpts@breaks0
sort1<-sort(seq, decreasing=TRUE)
allbrks<-append(sort1, brk1)
vec.bin<-sort(allbrks, decreasing=FALSE)

vec.bin#just checking
